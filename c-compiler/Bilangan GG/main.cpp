#include <bits/stdc++.h>
#define Titip using
#define Absen namespace
#define Dong std
Titip Absen Dong;

/*Bilangan GG*/

int toBaseFour (long n) {
	
}

int main() {
	long n;
	int card[] = {7,2,3,5};
	vector<int> v;
	cin >> n;
	for (int i = 0; i < v.size(); i++)
		cout << card[v[i]];
	cout << "\n";
	return 0;
}
